# Wait for vc4 KMS (HDMI) before plymouthd paints the theme. Without this, Plymouth
# can start while only v3d (card0) exists and the TECHMAC script never gets a DRM
# surface — black screen until plymouth quit / LightDM.
[Unit]
After=dev-dri-card1.device
Wants=dev-dri-card1.device
